extends ../layout

block content
  nav.navbar.navbar-default
    .container-fluid
      .navbar-header
        button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#bs-example-navbar-collapse-1', aria-expanded='false')
          span.sr-only Toggle navigation
          span.icon-bar
          span.icon-bar
          span.icon-bar
        a.navbar-brand(href='/recipes') Recipes
      ul.nav.navbar-nav.navbar-right
        li
          h4.navbar-text 
            i(class='glyphicon glyphicon-user')  
              span.user User
        li
          a(href='/users/logout') Log Out
  .container.recipeContainer
    .recipeList
      h3.text-center.text-danger hello {{ currentUser }}
      form.navbar-form.navbar-left(role='search')
        .form-group
          input.form-control(ng-model='search', type='text', placeholder='Search')
      .recipe(ng-repeat='recipe in recipes | filter: search')
        h3 {{ recipe.name }}
        .ingredients(ng-repeat='ingredient in recipe.ingredients')
          p {{ ingredient }}
        .directions
          p.text-primary {{ recipe.directions }}
    .recipeDetail